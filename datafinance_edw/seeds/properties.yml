version: 2

seeds:
  - name: unoserv
    config:
      column_types:
        DATA: varchar
        HISTÓRICO: varchar
        DOCUMENTO: varchar
        CRÉDITO: varchar  # Com acento
        DÉBITO: varchar   # Com acento
        SALDO: varchar
        CÓDIGO: varchar   # Com acento
        "CLIENTE / FORNECEDOR": varchar
